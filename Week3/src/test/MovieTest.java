package test;

import model.MovieController;

public class MovieTest {
    public static void main(String[] args) {
        MovieController controller = new MovieController();
        controller.start();
    }
}

